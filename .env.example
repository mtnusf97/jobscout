# Optional developer overrides — everything normally lives in the UI / data dir.
# Copy to .env to use. JOBSCOUT_DATA_DIR moves the DB, secret key, and file store.
#JOBSCOUT_DATA_DIR=/absolute/path/to/data
#JOBSCOUT_HOST=127.0.0.1
#JOBSCOUT_PORT=8000
